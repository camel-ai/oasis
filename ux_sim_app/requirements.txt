# OASIS UX Simulation App – Python dependencies
# Install with: pip install -r requirements.txt
# Then: playwright install chromium
# Then: python -m cloakbrowser install  (pre-downloads the stealth Chromium binary)

# Core
gradio>=4.20.0
httpx>=0.27.0
beautifulsoup4>=4.12.0
python-dotenv>=1.0.0

# Image handling (for visual analysis)
pillow>=10.0.0

# Browser automation (Mode 2 + UX screenshots)
playwright>=1.44.0

# Stealth Chromium — passes Cloudflare Turnstile, reCAPTCHA v3, FingerprintJS
# Binary (~200MB) auto-downloads on first run. Pre-download with:
#   python -m cloakbrowser install
cloakbrowser[geoip]>=0.3.0

# Async utilities
anyio>=4.0.0
